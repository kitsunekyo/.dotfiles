[user]
	name = Alex Spieslechner
	email = alexander.spieslechner@gmail.com
[alias]
	rsync = reset --hard @{u}
	fixup = "!git rsync log -n 50 --pretty=format:'%h %s' --no-merges | fzf | cut -c -7 | xargs -o git commit --fixup"
